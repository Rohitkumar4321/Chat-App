# Chat App (React Native + Node.js) - Starter

This zip contains a minimal starter for a real-time 1:1 chat app using React Native (Expo) and Node.js (Express + Socket.IO) with MongoDB (Mongoose).

Folders:
- server/: backend
- mobile/: Expo React Native app

Follow server/README.md and mobile/README.md to run each side.